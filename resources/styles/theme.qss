/* Shared theme TEMPLATE for the whole app - "AttendU" direction (see
   reference-theme/student-attendance-app-redesign-prompt.md and
   reference-theme/ASSUMPTIONS.md): a dark-first look with a purple-to-blue
   gradient on every primary CTA, 16px card radius, fully-rounded pill
   controls, and a sidebar / auth-left-panel that stay fixed-dark chrome
   regardless of the light/dark toggle (confirmed from the reference
   screenshots). Replaces the earlier "Kintsugi" white/black-chrome
   direction.

   This file is NOT consumed directly by the app - it's rendered into the
   real resources/styles/theme.qss (light) and resources/styles/theme_dark.qss
   (dark) by scripts/generate_theme.py, which fills in every double-curly
   placeholder from shared/palette.py's PALETTE (light output) or
   DARK_PALETTE (dark output), plus the theme-independent SPACING/RADIUS
   scales. Edit THIS file and re-run the script - never hand-edit the
   generated theme.qss/theme_dark.qss, they'll be overwritten.

   Each placeholder is substituted by NAME, not by matching a hex value, so
   two rules that happen to want the same light-mode color but different
   dark-mode colors (or vice versa) can't collide the way a blind
   hex-substitution generator would.

   Some literal `#FFFFFF`-style colors below are intentionally NOT
   tokenized: they're "always white text on a colored control" (primary-
   button labels, the notification badge, active sidebar-nav text) that
   should stay pure white in both themes, not resolve through the bg_card
   token (which does change between themes).

   Card/control radius: card=16px  control_sm=10px
   pill/avatar controls use `border-radius: 999px` (999) on a
   widget with a fixed/min height, so it renders as a true pill.

   Buttons use a `variant` dynamic Qt property (button.setProperty("variant",
   "primary"/"secondary"/"destructive"/"ghost")) matched via
   `QPushButton[variant="..."]` selectors - this is the screen-agnostic way
   to style a button. A few screens out of scope for this redesign pass
   (Settings/Statistics/Search/Profile - see ASSUMPTIONS.md) still use older
   objectName-chain selectors further down this file; both coexist.

   Error/active/state on widgets is driven by dynamic Qt properties set in
   Python: widget.setProperty("error"/"active"/"state", value);
   widget.style().unpolish(widget); widget.style().polish(widget) -
   which is what makes the [error="true"], [active="true"], [state="..."]
   selectors below take effect. */

* {
    font-family: "DM Sans", "Segoe UI", sans-serif;
}

QWidget#background {
    background-color: #EEF0F5;
}

QStackedWidget#stackedWidget {
    background-color: #EEF0F5;
}

/* Sidebar (and the auth screens' left brand panel, styled separately by
   objectName as each screen is built) are fixed-dark chrome in BOTH
   themes - see shared/palette.py's bg_sidebar token, which is identical
   in PALETTE and DARK_PALETTE on purpose. */
QWidget#sidebar_widget {
    background-color: #12141C;
    border-right: 1px solid #1F2333;
}

/* --- Auth screens (Login / Create Account / Reset Password) ---
   Left brand panel is the same fixed-dark chrome as the sidebar, with a
   soft purple glow anchored near the top-left corner (qradialgradient is
   the closest QSS equivalent to the reference's radial CSS glow). Right
   panel is the theme-reactive form surface. */

QWidget#auth_left_panel {
    background: qradialgradient(cx:0.18, cy:0.15, radius:1.1, fx:0.18, fy:0.15,
                 stop:0 #241B4A, stop:0.55 #12141C, stop:1 #12141C);
}

QWidget#auth_right_panel {
    background-color: #EEF0F5;
}

QLabel#auth_eyebrow_lbl {
    color: #9D90FA;
    font-size: 9pt;
    font-weight: 700;
}

QLabel#auth_headline_lbl {
    font-size: 26pt;
    font-weight: 700;
}

QLabel#auth_supporting_lbl {
    color: #767D91;
    font-size: 10pt;
    padding-top: 10px;
}

QLabel#auth_stat_1_lbl,
QLabel#auth_stat_2_lbl,
QLabel#auth_stat_3_lbl {
    background-color: #1B202C;
    color: #C7CCDA;
    font-size: 8pt;
    font-weight: 600;
    border: 1px solid #2A2F40;
    border-radius: 999px;
    padding: 6px 8px;
}

QLabel#auth_footer_lbl {
    color: #565D70;
    font-size: 8.5pt;
}

QPushButton#theme_toggle_btn {
    background-color: #FFFFFF;
    border: 1px solid #E4E7ED;
    border-radius: 18px;
}

QPushButton#theme_toggle_btn:hover {
    background-color: #F4F5F9;
}

QLabel#signin_email_field_lbl,
QLabel#signin_password_field_lbl,
QLabel#signup_email_field_lbl,
QLabel#signup_password_field_lbl,
QLabel#reset_email_field_lbl,
QLabel#name_field_lbl,
QLabel#surname_field_lbl,
QLabel#confirm_password_field_lbl,
QLabel#security_question_field_lbl,
QLabel#security_question_2_field_lbl,
QLabel#new_password_field_lbl,
QLabel#confirm_new_password_field_lbl,
QLabel#create_account_prompt_lbl,
QLabel#sign_in_prompt_lbl {
    color: #8A93A7;
    font-size: 9.5pt;
}

QPushButton#back_to_signin_btn {
    text-align: left;
    padding-left: 0;
}

QWidget#top_bar_widget,
QWidget#class_header_widget {
    background-color: #FFFFFF;
    border-bottom: 1px solid #E4E7ED;
}

QWidget#topSideWidget {
    background-color: #FFFFFF;
}

QWidget#addNewClass {
    background-color: #EEF0F5;
}

/* --- Add/Edit Class wizard --- */

QLabel#wizard_title_lbl {
    font-size: 18pt;
    font-weight: 700;
    color: #1B1E2B;
}

QLabel#step_dot_1_lbl,
QLabel#step_dot_2_lbl,
QLabel#step_dot_3_lbl {
    color: #8A93A7;
    font-size: 9.5pt;
    font-weight: 600;
    padding: 6px 4px;
}

QLabel#step_dot_1_lbl[active="true"],
QLabel#step_dot_2_lbl[active="true"],
QLabel#step_dot_3_lbl[active="true"] {
    color: #7C6EF7;
    border-bottom: 2px solid #7C6EF7;
}

QFrame#class_info_card,
QFrame#color_row_widget,
QFrame#danger_zone_card {
    background-color: #FFFFFF;
    border-radius: 16px;
    border: 1px solid #E4E7ED;
}

QFrame#danger_zone_card {
    border: 1px solid #FCA5A5;
}

QLabel#danger_zone_caption_lbl {
    color: #8A93A7;
    font-size: 9pt;
}

/* Dialog/page roots that show generous margins around their cards (rather
   than filling edge-to-edge) need an explicit background - otherwise Qt's
   default palette shows through, which is fine in light mode by coincidence
   but reads as a jarring unstyled light strip in dark mode. */
QWidget#ClassWindow,
QWidget#TakeAttendanceWindow {
    background-color: #EEF0F5;
}

/* Text wordmark, fixed-dark chrome like the rest of the sidebar/auth-panel
   it sits on - always uses the dark-mode accent tint regardless of theme. */
QLabel#auth_wordmark_lbl,
QLabel#sidebar_wordmark_lbl {
    color: #9D90FA;
    font-size: 14pt;
    font-weight: 700;
}

/* --- Cards / panels --- */

QFrame#card_frame,
QFrame#settings_card_frame,
QFrame#profile_card_frame,
QFrame#class_details_card,
QFrame#schedule_card,
QFrame#notes_card,
QFrame#roster_section_card,
QFrame#statistics_card,
QFrame#dashboard_stat_card,
QFrame#control_strip_card {
    background-color: #FFFFFF;
    border-radius: 16px;
    border: 1px solid #E4E7ED;
}

/* --- Dashboard --- */

QLabel#dashboard_greeting_lbl {
    font-size: 20pt;
    font-weight: 700;
    color: #1B1E2B;
}

QLabel#dashboard_date_lbl {
    font-size: 9.5pt;
    color: #8A93A7;
}

QLabel#dashboard_stat_value_lbl {
    font-size: 20pt;
    font-weight: 700;
    color: #1B1E2B;
}

QLabel#dashboard_pinned_title_lbl,
QLabel#dashboard_active_title_lbl,
QLabel#dashboard_inactive_title_lbl,
QLabel#today_classes_title_lbl,
QLabel#recently_viewed_title_lbl {
    font-size: 9pt;
    font-weight: 700;
    color: #8A93A7;
    padding-top: 8px;
}

QLabel#dashboard_empty_state_lbl,
QLabel#no_classes_today_lbl,
QLabel#no_recently_viewed_lbl {
    font-size: 10pt;
    color: #8A93A7;
    padding: 24px;
    qproperty-alignment: AlignCenter;
}

/* --- Today's Classes / Recently Viewed rows (Dashboard) --- */

QFrame#today_class_row_widget,
QFrame#recently_viewed_row_widget {
    background-color: #F4F5F9;
    border-radius: 10px;
}

QLabel#today_class_label_lbl,
QLabel#recently_viewed_label_lbl {
    font-size: 9.5pt;
    font-weight: 600;
    color: #1B1E2B;
}

QLabel#my_classes_count_lbl {
    font-size: 9.5pt;
    color: #8A93A7;
}

/* --- Segmented filter tabs (Dashboard) --- */

QPushButton[variant="segmented"] {
    background-color: #FFFFFF;
    color: #8A93A7;
    border: 1px solid #E4E7ED;
    border-radius: 999px;
    padding: 7px 16px;
    font-size: 9.5pt;
}

QPushButton[variant="segmented"]:checked {
    background-color: #EEECFE;
    color: #7C6EF7;
    border: 1px solid #7C6EF7;
    font-weight: 600;
}

/* --- Class color swatch (Add/Edit Class) --- */

QFrame#class_color_swatch {
    border: 1px solid #D4D8E2;
    border-radius: 6px;
}

/* --- Typography --- */

/* Fallback for every QLabel with no more specific #id rule below - without
   this, unstyled labels inherit Qt's default (near-black) text color,
   which is invisible against a dark-mode background. */
QLabel {
    color: #1B1E2B;
}

QLabel#title_lbl,
QLabel#title_lbl_settings,
QLabel#statistics_title_lbl,
QLabel#my_classes_title_lbl,
QLabel#signin_title_lbl,
QLabel#signup_title_lbl,
QLabel#reset_title_lbl {
    font-size: 20pt;
    font-weight: 700;
    color: #1B1E2B;
}

QLabel#signin_subtitle_lbl,
QLabel#signup_subtitle_lbl,
QLabel#reset_subtitle_lbl,
QLabel#class_code_lbl,
QLabel#class_row_caption_lbl,
QLabel#login_error_lbl,
QLabel#empty_state_lbl,
QLabel#statistics_empty_lbl,
QLabel#roster_status_lbl,
QLabel#class_breadcrumb_lbl {
    font-size: 9pt;
    color: #8A93A7;
}

/* Sidebar user-identity labels are fixed-dark chrome (like the sidebar
   itself, see bg_sidebar) - text_primary/text_secondary are theme-reactive
   and would go near-invisible (dark-on-dark) once the sidebar stopped
   flipping to white in light mode. */
QLabel#sidebar_user_name_lbl,
QLabel#sidebar_top_name_lbl {
    font-size: 9.5pt;
    font-weight: 600;
    color: #C7CCDA;
}

QLabel#sidebar_user_email_lbl {
    font-size: 9pt;
    color: #767D91;
}

QWidget#sidebar_profile_widget {
    border-radius: 10px;
}

QWidget#sidebar_profile_widget:hover {
    background-color: #1B202C;
}

/* --- Progress/session badges (Take Attendance) --- */

QLabel#attendance_progress_lbl {
    background-color: #EEECFE;
    color: #7C6EF7;
    font-size: 9pt;
    font-weight: 600;
    border-radius: 999px;
    padding: 4px 12px;
}

QLabel#session_countdown_lbl {
    color: #7C6EF7;
    font-size: 9pt;
    font-weight: 600;
}

QLabel#roster_status_lbl {
    font-size: 10pt;
    padding: 16px;
}

QLabel#login_error_lbl[error="true"] {
    color: #DC2626;
}

QLabel#empty_state_lbl,
QLabel#statistics_empty_lbl {
    font-size: 10pt;
    padding: 20px;
    qproperty-alignment: AlignCenter;
}

QLabel#class_row_caption_lbl {
    padding-left: 8px;
    padding-bottom: 6px;
}

/* --- Weekly schedule grid --- */

QLabel#schedule_day_header_lbl {
    font-weight: 600;
    padding: 4px 8px;
}

QLabel#schedule_slot_chip_lbl {
    background-color: #EEECFE;
    color: #7C6EF7;
    border-radius: 999px;
    padding: 4px 10px;
    margin: 2px;
}

/* --- Notes (Class Window) --- */

QTextEdit#class_notes_edit {
    background-color: #F4F5F9;
    border: 1.5px solid transparent;
    border-radius: 10px;
    padding: 10px 14px;
    font-size: 10pt;
    color: #1B1E2B;
}

QTextEdit#class_notes_edit:focus {
    border: 1.5px solid #7C6EF7;
    background-color: #FFFFFF;
}

/* --- At-risk callout (Class Window) --- */

QWidget#at_risk_widget {
    background-color: #FEF3C7;
    border: 1px solid #FCD34D;
    border-radius: 10px;
}

QLabel#at_risk_title_lbl {
    font-weight: 700;
    font-size: 10pt;
    color: #92400E;
}

QLabel#at_risk_students_lbl {
    font-size: 9pt;
    color: #92400E;
}

/* --- Notifications badge --- */

QLabel#notifications_badge_lbl {
    background-color: #DC2626;
    color: #FFFFFF;
    font-size: 8pt;
    font-weight: 600;
    border-radius: 8px;
    min-width: 16px;
    max-width: 16px;
    min-height: 16px;
    max-height: 16px;
    qproperty-alignment: AlignCenter;
}

/* --- Password strength indicator - small pill badge --- */

QLabel#password_strength_lbl,
QLabel#new_password_strength_lbl,
QLabel#settings_password_strength_lbl {
    font-size: 8pt;
    font-weight: 600;
    padding: 2px 10px;
    border-radius: 999px;
    max-width: 64px;
    qproperty-alignment: AlignCenter;
}

QLabel#password_strength_lbl[strength="weak"],
QLabel#new_password_strength_lbl[strength="weak"],
QLabel#settings_password_strength_lbl[strength="weak"] {
    color: #991B1B;
    background-color: #FEE2E2;
}

QLabel#password_strength_lbl[strength="medium"],
QLabel#new_password_strength_lbl[strength="medium"],
QLabel#settings_password_strength_lbl[strength="medium"] {
    color: #92400E;
    background-color: #FEF3C7;
}

QLabel#password_strength_lbl[strength="strong"],
QLabel#new_password_strength_lbl[strength="strong"],
QLabel#settings_password_strength_lbl[strength="strong"] {
    color: #166534;
    background-color: #DCFCE7;
}

QLabel#change_password_section_lbl,
QLabel#security_question_section_lbl,
QLabel#roster_section_title_lbl,
QLabel#class_details_title_lbl,
QLabel#statistics_filter_lbl,
QLabel#search_status_lbl,
QLabel#title_lbl_2,
QLabel#security_question_lbl,
QLabel#security_question_2_lbl {
    font-size: 12pt;
    font-weight: 600;
    color: #1B1E2B;
}

QLabel#class_name_header_lbl,
QLabel#attendance_page_title_lbl {
    font-size: 20pt;
    font-weight: 700;
    color: #1B1E2B;
}

QLabel#attendance_class_name_lbl {
    font-size: 10.5pt;
    color: #8A93A7;
}

QPushButton#back_to_class_btn {
    background-color: transparent;
    border: none;
    color: #8A93A7;
    font-size: 9.5pt;
    font-weight: 600;
    text-align: left;
    padding: 2px 0;
}

QPushButton#back_to_class_btn:hover {
    color: #7C6EF7;
}

QLabel[fieldLabel="true"] {
    font-size: 10pt;
    color: #8A93A7;
}

QLabel[fieldValue="true"] {
    font-size: 10pt;
    font-weight: 600;
    color: #1B1E2B;
}

/* --- Inputs - filled pill style (replaces the old underline-only look) --- */

QLineEdit {
    background-color: #F4F5F9;
    border: 1.5px solid transparent;
    border-radius: 999px;
    padding: 6px 16px;
    font-size: 10.5pt;
    color: #1B1E2B;
}

QLineEdit:focus {
    border: 1.5px solid #7C6EF7;
    background-color: #FFFFFF;
}

QLineEdit[error="true"] {
    border: 1.5px solid #DC2626;
}

QLineEdit:read-only {
    color: #8A93A7;
    background-color: transparent;
}

QLabel[error="true"] {
    color: #DC2626;
    font-size: 9pt;
}

QComboBox {
    background-color: #F4F5F9;
    border: 1.5px solid transparent;
    border-radius: 999px;
    padding: 6px 16px;
    font-size: 10.5pt;
}

QComboBox:focus {
    border: 1.5px solid #7C6EF7;
    background-color: #FFFFFF;
}

/* --- Button variant system (new) - QPushButton[variant="..."] ---
   Screens migrated to the redesign set this property in Python instead of
   relying on an objectName chain. */

QPushButton[variant="primary"] {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:0,
                 stop:0 #7C6EF7, stop:1 #6D8CFA);
    color: #FFFFFF;
    border: none;
    border-radius: 999px;
    padding: 10px 22px;
    font-size: 10.5pt;
    font-weight: 600;
}

QPushButton[variant="primary"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:0,
                 stop:0 #6C5CE1, stop:1 #6D8CFA);
}

QPushButton[variant="primary"]:pressed {
    background: #5B4FD1;
}

QPushButton[variant="primary"]:disabled {
    background: #EEECFE;
    color: #C2C7D4;
}

QPushButton[variant="secondary"] {
    background-color: transparent;
    color: #7C6EF7;
    border: 1.5px solid #7C6EF7;
    border-radius: 999px;
    padding: 10px 22px;
    font-size: 10.5pt;
}

QPushButton[variant="secondary"]:hover {
    background-color: #EEECFE;
}

QPushButton[variant="ghost"] {
    background-color: transparent;
    color: #8A93A7;
    border: none;
    border-radius: 999px;
    padding: 8px 14px;
    font-size: 9.5pt;
}

QPushButton[variant="ghost"]:hover {
    background-color: #F4F5F9;
    color: #1B1E2B;
}

QPushButton[variant="destructive"] {
    background-color: #FEE2E2;
    color: #991B1B;
    border: 1px solid #FCA5A5;
    border-radius: 999px;
    padding: 10px 22px;
    font-size: 10.5pt;
    font-weight: 600;
}

QPushButton[variant="destructive"]:hover {
    background-color: #FCA5A5;
}

/* --- Primary filled buttons (blue) - legacy objectName chain, still used
   by screens not yet migrated to the variant system --- */

QPushButton#save_profile_btn,
QPushButton#change_password_btn,
QPushButton#update_security_question_btn,
QPushButton#create_new_class_btn {
    background-color: #7C6EF7;
    color: #FFFFFF;
    border: none;
    border-radius: 10px;
    padding: 8px 14px;
    font-size: 10pt;
    font-weight: 600;
}

QPushButton#save_profile_btn:hover,
QPushButton#change_password_btn:hover,
QPushButton#update_security_question_btn:hover,
QPushButton#create_new_class_btn:hover {
    background-color: #6C5CE1;
}

QPushButton#save_profile_btn:pressed,
QPushButton#change_password_btn:pressed,
QPushButton#update_security_question_btn:pressed,
QPushButton#create_new_class_btn:pressed {
    background-color: #5B4FD1;
}

/* --- Secondary outline buttons (blue) - legacy objectName chain --- */

QPushButton#edit_profile_btn {
    background-color: transparent;
    color: #7C6EF7;
    border: 1px solid #7C6EF7;
    border-radius: 10px;
    padding: 8px 14px;
    font-size: 10pt;
}

QPushButton#edit_profile_btn:hover {
    background-color: #EEECFE;
}

/* --- Destructive buttons - legacy objectName chain ---
   delete_account_btn stays a stronger (tinted, not solid) warning since it's
   the highest-stakes action in the app. --- */

QPushButton#delete_account_btn {
    background-color: #FEE2E2;
    color: #991B1B;
    border: 1px solid #FCA5A5;
    border-radius: 10px;
    padding: 8px 14px;
    font-size: 10pt;
    font-weight: 600;
}

QPushButton#delete_account_btn:hover {
    background-color: #FCA5A5;
}

QPushButton#delete_account_btn:pressed {
    background-color: #DC2626;
}

QLabel#danger_zone_lbl {
    color: #DC2626;
    font-size: 11px;
    font-weight: 600;
}

/* Small inline "X" delete button on each class row (My Classes / Search) */
QPushButton#class_delete_btn {
    background-color: transparent;
    color: #DC2626;
    border: none;
    font-weight: 600;
}

QPushButton#class_delete_btn:hover {
    background-color: #FEE2E2;
    border-radius: 4px;
}

/* --- Links / icon-only toggles --- */

QPushButton#forgot_password_btn {
    background-color: transparent;
    border: none;
    color: #7C6EF7;
    font-size: 9pt;
    text-align: left;
}

QPushButton#forgot_password_btn:hover {
    color: #6C5CE1;
    text-decoration: underline;
}

QPushButton#close_window_btn {
    background-color: transparent;
    border: none;
    border-radius: 14px;
}

QPushButton#close_window_btn:hover {
    background-color: rgba(220, 38, 38, 15);
}

QToolButton {
    background-color: transparent;
    border: none;
    color: #7C6EF7;
    font-size: 8pt;
}

QToolButton:hover {
    color: #6C5CE1;
}

QCheckBox {
    spacing: 6px;
    font-size: 9pt;
    color: #8A93A7;
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border-radius: 5px;
    border: 1.5px solid #D4D8E2;
    background: #FFFFFF;
}

QCheckBox::indicator:hover {
    border: 1.5px solid #7C6EF7;
}

QCheckBox::indicator:checked {
    background: #7C6EF7;
    border: 1.5px solid #7C6EF7;
}

/* --- Class row (My Classes / Search) --- */

QFrame#class_row_widget {
    background-color: #FFFFFF;
    border: 1px solid #E4E7ED;
    border-radius: 10px;
}

QFrame#class_row_widget:hover {
    border: 1px solid #EEECFE;
}

QPushButton#class_row_name_btn {
    background-color: transparent;
    border: none;
    text-align: left;
    color: #1B1E2B;
    font-size: 10.5pt;
    font-weight: 600;
    padding: 0;
}

QPushButton#class_row_name_btn:hover {
    color: #7C6EF7;
}

/* --- Dashboard class card + shared expandable Properties panel --- */

QFrame#dashboard_class_card {
    background-color: #FFFFFF;
    border: 1px solid #E4E7ED;
    border-radius: 16px;
}

QFrame#dashboard_class_card:hover {
    border: 1px solid #EEECFE;
}

QPushButton#class_card_title_btn {
    background-color: transparent;
    border: none;
    text-align: left;
    color: #1B1E2B;
    font-size: 10.5pt;
    font-weight: 700;
    padding: 0;
}

QPushButton#class_card_title_btn:hover {
    color: #7C6EF7;
}

QFrame#class_detail_panel {
    background-color: #F4F5F9;
    border: none;
    border-bottom-left-radius: 10px;
    border-bottom-right-radius: 10px;
}

QLabel#class_card_percent_lbl {
    font-size: 10pt;
    font-weight: 700;
}

QLabel#class_card_percent_lbl[tier="success"] { color: #16A34A; }
QLabel#class_card_percent_lbl[tier="warning"] { color: #B45309; }
QLabel#class_card_percent_lbl[tier="error"] { color: #DC2626; }

QProgressBar#class_card_progress_bar {
    background-color: #F4F5F9;
    border: none;
    border-radius: 3px;
}

QProgressBar#class_card_progress_bar::chunk[tier="success"] { background-color: #16A34A; border-radius: 3px; }
QProgressBar#class_card_progress_bar::chunk[tier="warning"] { background-color: #B45309; border-radius: 3px; }
QProgressBar#class_card_progress_bar::chunk[tier="error"] { background-color: #DC2626; border-radius: 3px; }

/* --- Attendance-rate strip (Class Detail) --- */

QWidget#attendance_rate_widget {
    background-color: #F4F5F9;
    border-radius: 10px;
}

QLabel#attendance_rate_lbl,
QLabel#attendance_rate_fraction_lbl {
    font-size: 9pt;
    font-weight: 600;
    color: #8A93A7;
}

QProgressBar#attendance_rate_bar {
    background-color: #E4E7ED;
    border: none;
    border-radius: 3px;
}

QProgressBar#attendance_rate_bar::chunk[tier="success"] { background-color: #16A34A; border-radius: 3px; }
QProgressBar#attendance_rate_bar::chunk[tier="warning"] { background-color: #B45309; border-radius: 3px; }
QProgressBar#attendance_rate_bar::chunk[tier="error"] { background-color: #DC2626; border-radius: 3px; }

QPushButton#card_icon_btn,
QPushButton#back_to_my_classes_btn,
QPushButton#refresh_student_list_btn {
    background-color: transparent;
    border: none;
    border-radius: 10px;
    font-size: 12pt;
    padding: 2px;
}

QPushButton#card_icon_btn:hover,
QPushButton#back_to_my_classes_btn:hover,
QPushButton#refresh_student_list_btn:hover {
    background-color: #F4F5F9;
}

/* --- Control strip (Take Attendance) --- */

QPushButton#date_toggle_btn {
    background-color: #F4F5F9;
    border: 1.5px solid transparent;
    border-radius: 999px;
    padding: 8px 16px;
    font-size: 9.5pt;
    font-weight: 600;
    color: #1B1E2B;
}

QPushButton#date_toggle_btn:hover {
    border-color: #7C6EF7;
}

QPushButton#date_toggle_btn:checked {
    background-color: #EEECFE;
    color: #7C6EF7;
    border-color: #7C6EF7;
}

QLabel#session_countdown_lbl {
    font-size: 9pt;
    font-weight: 600;
    color: #7C6EF7;
}

QLabel#todays_summary_lbl {
    font-size: 9pt;
    color: #8A93A7;
}

/* --- RFID scan status card (Take Attendance) --- */

QFrame#scan_status_card {
    background-color: #F4F5F9;
    border: 1px solid #E4E7ED;
    border-radius: 10px;
}

QFrame#scan_status_card[state="success"] {
    background-color: #DCFCE7;
    border-color: #86EFAC;
}

QFrame#scan_status_card[state="warning"] {
    background-color: #FEF3C7;
    border-color: #FCD34D;
}

QFrame#scan_status_card[state="error"] {
    background-color: #FEE2E2;
    border-color: #FCA5A5;
}

QLabel#scan_status_text_lbl {
    font-size: 10pt;
    color: #1B1E2B;
}

QLabel#scan_status_icon_lbl {
    font-size: 14pt;
}

/* --- Sidebar navigation - fixed-dark chrome, same in both themes --- */

QPushButton#dashboard_btn,
QPushButton#my_classes_btn,
QPushButton#statistics_btn,
QPushButton#settings_btn,
QPushButton#log_out_btn {
    background-color: transparent;
    color: #767D91;
    text-align: left;
    border: none;
    border-radius: 10px;
    padding: 9px 12px;
    font-size: 10.5pt;
}

QPushButton#dashboard_btn:hover,
QPushButton#my_classes_btn:hover,
QPushButton#statistics_btn:hover,
QPushButton#settings_btn:hover,
QPushButton#log_out_btn:hover {
    background-color: #1B202C;
    color: #C7CCDA;
}

QPushButton#dashboard_btn:pressed,
QPushButton#my_classes_btn:pressed,
QPushButton#statistics_btn:pressed,
QPushButton#settings_btn:pressed,
QPushButton#log_out_btn:pressed {
    background-color: #1F2333;
}

QPushButton#dashboard_btn[active="true"],
QPushButton#my_classes_btn[active="true"],
QPushButton#statistics_btn[active="true"],
QPushButton#settings_btn[active="true"] {
    background-color: #1F2333;
    color: #FFFFFF;
    font-weight: 600;
}

QLabel#sidebar_role_lbl {
    color: #767D91;
    font-size: 8.5pt;
}

QPushButton#profile_btn {
    background-color: transparent;
    border: none;
    border-radius: 17px;
    padding: 0;
}

QPushButton#profile_btn:hover {
    background-color: #F4F5F9;
}

QPushButton#notifications_btn {
    background-color: transparent;
    border: none;
    border-radius: 17px;
}

QPushButton#notifications_btn:hover {
    background-color: #F4F5F9;
}

QPushButton#theme_toggle_btn {
    background-color: transparent;
    border: 1px solid #E4E7ED;
    border-radius: 17px;
}

QPushButton#theme_toggle_btn:hover {
    background-color: #F4F5F9;
}

QLabel#server_health_lbl {
    background-color: #F4F5F9;
    border-radius: 999px;
    padding: 6px 14px;
    font-size: 9pt;
    font-weight: 600;
}

/* --- Tables --- */

QTableWidget {
    background-color: #FFFFFF;
    border: 1px solid #E4E7ED;
    border-radius: 10px;
    gridline-color: #E4E7ED;
    alternate-background-color: #F4F5F9;
    font-size: 10pt;
}

QTableWidget::item {
    padding: 6px;
}

QTableWidget::item:hover {
    background-color: #F4F5F9;
}

QTableWidget::item:selected {
    background-color: #EEECFE;
    color: #1B1E2B;
}

QHeaderView::section {
    background-color: #F4F5F9;
    color: #8A93A7;
    padding: 6px;
    border: none;
    border-bottom: 2px solid #E4E7ED;
    font-weight: 600;
    font-size: 10pt;
}

/* --- Calendar --- */

QCalendarWidget QToolButton {
    background-color: transparent;
    color: #1B1E2B;
    border-radius: 6px;
    padding: 4px;
}

QCalendarWidget QToolButton:hover {
    background-color: #EEECFE;
}

QCalendarWidget QAbstractItemView:enabled {
    selection-background-color: #7C6EF7;
    selection-color: #FFFFFF;
}

QCalendarWidget QWidget#qt_calendar_navigationbar {
    background-color: #F4F5F9;
}

/* --- Weekday cards (Add/Edit Class wizard, Schedule step) --- */

QFrame#mondayGroupBox,
QFrame#tuesdayGroupBox,
QFrame#wednesdayGroupBox,
QFrame#thursdayGroupBox,
QFrame#fridayGroupBox {
    background-color: #F4F5F9;
    border: 1px solid #E4E7ED;
    border-radius: 10px;
}

/* --- Popup menus (notifications bell, profile avatar) --- */

QMenu {
    background-color: #FFFFFF;
    border: 1px solid #E4E7ED;
    border-radius: 10px;
    padding: 6px;
}

QMenu::item {
    padding: 8px 16px;
    border-radius: 10px;
    color: #1B1E2B;
}

QMenu::item:selected {
    background-color: #F4F5F9;
}

QMenu::item:disabled {
    color: #8A93A7;
}

QMenu::separator {
    height: 1px;
    background-color: #E4E7ED;
    margin: 6px 8px;
}

/* --- Message boxes --- */

QMessageBox {
    background-color: #FFFFFF;
    border-radius: 16px;
}

QMessageBox QPushButton {
    background-color: #7C6EF7;
    color: #FFFFFF;
    border: none;
    border-radius: 999px;
    padding: 6px 18px;
    min-width: 64px;
}

QMessageBox QPushButton:hover {
    background-color: #6C5CE1;
}

/* --- Scrollbars --- */

QScrollBar:vertical {
    background: #F4F5F9;
    width: 10px;
    margin: 2px;
    border-radius: 5px;
}

QScrollBar::handle:vertical {
    background: #D4D8E2;
    border-radius: 5px;
    min-height: 24px;
}

QScrollBar::handle:vertical:hover {
    background: #C2C7D4;
}

QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0;
}

QScrollBar:horizontal {
    background: #F4F5F9;
    height: 10px;
    margin: 2px;
    border-radius: 5px;
}

QScrollBar::handle:horizontal {
    background: #D4D8E2;
    border-radius: 5px;
    min-width: 24px;
}

QScrollBar::add-line:horizontal,
QScrollBar::sub-line:horizontal {
    width: 0;
}
